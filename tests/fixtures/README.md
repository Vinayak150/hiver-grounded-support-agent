# Synthetic test fixture

`twcs_mini.csv` is deliberately small, synthetic test data. It exists only to exercise deterministic Phase 1 parsing and thread-reconstruction tests. It is not TWCS, an evaluation set, a golden set, or a source of any project result.
